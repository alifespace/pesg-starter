{
  "name": "@essg/nuxt01",
  "version": "0.08.31",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "pnpm run clean && nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "clean": "rimraf dist",
    "cf-build": "pnpm run build",
    "cf-dev": "pnpm run build && wrangler pages dev dist",
    "cf-deploy": "pnpm run cf-build && wrangler pages deploydist"
  },
  "dependencies": {
    "nuxt": "^4.0.3",
    "vue": "^3.5.20",
    "vue-router": "^4.5.1"
  }
}